'use strict';

describe('validate directive', function(){
        
  it('blurイベントでvalidateサービスの呼び出し', function(){
    // var userService = jasmine.createSpyObj('userService', ['validate']);
    // module('simple-taskboard.webui.components.user', function($provide){
    //   $provide.value('userService', userService);
    // });
    // 
    // var $compile, $rootScope, $q;
    // inject(function(_$compile_, _$rootScope_, _$q_){
    //   $compile = _$compile_;
    //   $rootScope = _$rootScope_;
    //   $q = _$q_;
    // });
    // 
    // var defered = $q.defer(); 
    // defered.resolve();
    // userService.validate.and.returnValue(defered.promise);
    // 
    // var scope = $rootScope.$new();
    // var input = $compile('<input type="text" ng-model="email" user-validate>')(scope);
    // input.prop('value', 'taro@test');
    // input.change();
    // input.blur();
    // 
    // expect(userService.validate).toHaveBeenCalledWith(
    //   jasmine.objectContaining({ email: "taro@test"}), ['email']);
    
  }); 
        
});
